
int fun() {
    a = fun();
    {}{}{}{{{}{{}}}{}};;;;;
    a = (a + b - ( c * d / ( e % b +  (g * h - (i - j))) / (a * (b % c))) * (a + b));

//    b = 5 - (b - 7 - (a - fun()) - 5) - 1;
    return 1 - 8;
    ;
    {;}
}

int cc = a - fun();

int a(){}

int main() {
    a = ++fun();
    k = (a--)--;
    int d = test() + 55 - a * 1 / fun();
    int a;
    short int9 = 0X0AA;
    long ___q999V__ssdfghjk = -1234590;
    long l = -1324;
    for(i = 0; i < 0xFF; i++){
        for(i = 0; i < 0xFF; i++)
            for(i = 0; i < 0xFF; i++)
                for(i = 0; i < 0xFF; i++)
                    for(i = 0; i < 0xFF; i++)
                         a = (3 + ((b + i) * 2) % 0xF) * int9 / l;
    }
    return a;
}
